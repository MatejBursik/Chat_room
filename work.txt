TODO:
    - add a proxy-server (nginx) infront of the web-server
    - make the database
        - UML (class diagram)
        - init database
    - make the web-application
        - pages (login, register, manage account, index, chatroom, create chatroom, manage chatroom)
    - make the API

FIX:
