TODO:
    - add a reverse-proxyserver (nginx) infront of the web-server
    - consider a micro-service for the API RestController
    - make the database
        - UML (class diagram)
        - init database
    - make the web-application
        - pages (login, register, manage account, index, chatroom, create chatroom, manage chatroom)
    - make the API

FIX:
